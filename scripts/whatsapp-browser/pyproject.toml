[project]
name = "whatsapp-browser"
version = "0.1.0"
description = "WhatsApp Web browser sender — cliente oficial (sin Baileys/Evolution)"
requires-python = ">=3.11"
dependencies = [
    "undetected-chromedriver>=3.5.5",
    "setuptools>=75",
]

[tool.uv]
package = false
